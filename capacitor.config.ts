import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cube.www',
  appName: 'cube-front',
  webDir: 'dist/cube-front',
  bundledWebRuntime: false
};

export default config;
